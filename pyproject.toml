[project]
name = "breakout-engine"
version = "0.1.0"
description = "Daily stock scanner for high-probability breakout setups using Qullamaggie VCP and Minervini Stage 2 methodology"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alpaca-py>=0.28.0",
    "dotenv>=0.9.9",
    "numpy>=2.0.0",
    "pandas>=3.0.0",
    "requests>=2.32.5",
    "scipy>=1.17.1",
    "yfinance>=0.2.54",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
